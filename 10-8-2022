#include<stdio.h>
int main()
{
	int n,sum=0,r,s,k;
	scanf("%d",&n);
	k=n;
	while(n>0)
	{
		r=n%10;
		sum+=r;
		n=n/10;
	}
	if(k%sum==0)
	{
		printf("harshad number");
	}
	else
	{
		printf("not harshad number");
	}
}
